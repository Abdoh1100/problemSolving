package com.mvc.codeForces.streams;

import java.util.ArrayList;
import java.util.List;

public class terminal_intermediate {

    public static void main (String []args){

        List<Integer> numbers=new ArrayList<>();
//        for (int i = 0; i <10 ; i++) {
//            numbers.add(i);
//        }
        numbers.add(1);
        numbers.add(2);
        numbers.add(3);
        numbers.add(4);
        numbers.add(5);
//        System.out.println(numbers);
//        numbers.stream()
//                .filter(value->value<4)
//                .map(i->i*2)
//                .filter(i->i>10)
//                .forEach(System.out::println);
//#####################################################
//        The exercise template includes a template for the method public static ArrayList<Integer> divisible(ArrayList<Integer> numbers)
//                . Implement a functionality there that gathers numbers divisible by two
//                , three or five from the list it receives as a parameter, and returns them as a new list
//                . The list received as a parameter must not be altered.
//        numbers.stream()
//                .filter(value->value%5==0||value%3==0||value%2==0)
//                .forEach(System.out::println);
//        ###############################################

//        int sum =numbers.stream()
//                .reduce(2,(prev,res)->prev+res);
//        System.out.println(sum);

    }
}
